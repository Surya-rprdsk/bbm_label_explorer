// This test file is intentionally left empty because the custom window menu bar and system tray menu logic have been removed.
// Jest requires at least one test, so we add a dummy test to prevent errors.
test('dummy', () => { expect(true).toBe(true); });
